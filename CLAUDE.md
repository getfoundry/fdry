# fdry Instructions

## Deploy
- Always deploy the frontend with Cloudflare.
- Do not use Vercel for fdry frontend deploys unless Lewis explicitly asks for Vercel.
- For the Vite frontend, run the frontend gate first, then deploy `frontend/dist` through Cloudflare Pages or the repo's Cloudflare pipeline.
